{
  "$comment": "MCP server deferred to v1.1 per docs/ROADMAP.md; this discovery file will gain an endpoint + tools entry when /api/mcp ships. Until then no endpoint field is advertised so agents do not chase a 404. See docs/AGENT_READINESS.md §6.3.",
  "name": "akaushik.org",
  "version": "0.0.0",
  "status": "planned",
  "description": "Portfolio MCP server — planned for v1.1. Will expose case-study lookup + availability tools."
}
