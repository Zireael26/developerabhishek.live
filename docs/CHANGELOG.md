# Changelog

All notable changes to developerabhishek.live are documented here. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html) for released milestones.

## [Unreleased]

### Added
- Phase 1 chrome slice (EPIC-01 §2, §3.1): sticky `SiteNav` with wordmark + six section links + status line + `ThemeToggle`, terminal `SiteFooter` with build-notes + commit SHA + license links, and a single-page `<main id="top">` that lays out eight section scaffolds (`#hero…#contact`) with `data-companion-pose` / `data-screen-label` anchors ready for The Wanderer. Section bodies land in per-slice PRs.
- `components/site/{SiteNav,SiteFooter,ThemeToggle}.tsx` — nav + footer are server components; `ThemeToggle` is the only client island in the chrome (flips `html[data-mode]`, persists to `localStorage['abhishek.portfolio.mode']`, respects `prefers-color-scheme` on first mount).
- `app/globals.css` — ported site-chrome rules verbatim from `_reference/portfolio/styles.css` (+ the inline `<style>` diffs for nav meta / theme toggle / companion). Design tokens unchanged.
- `#companion` mount point in `<body>` so the Wanderer slice has a place to render without touching chrome again.
- `public/init-theme.js` — pre-hydration theme bootstrap. Sync `<script src>` in `<head>` reads `localStorage['abhishek.portfolio.mode']` / `prefers-color-scheme` and sets `html[data-mode]` before first paint. Prevents a light-mode flash for dark-preference users before `ThemeToggle`'s effect reconciles.
- Phase 1 hero slice (EPIC-01 §2, §3.2): `components/sections/Hero.tsx` renders the 88px Newsreader hero title with three tagline variants (A visible, B/C carry the `hidden` attr ready for TweakBridge to swap), the `hero-lede` sub-copy, primary + ghost CTAs, the four-item `hero-facts` DL with `data-stat="total"` injected from `public/data/stats.json` at build time, a static SVG agent-graph scene inside the framed `.scene-frame`, and the sticky hero marquee (stack/tooling names, duplicated track for seamless CSS loop).
- `app/globals.css` — ported hero-specific rules from the reference: `.hero`, `.hero-grid`, `.hero-title` (+ tagline-a `em` accent), `.hero-lede`, `.hero-cta-row`, `.hero-facts` / `dt` / `dd`, `.scene-frame`, `.scene-label` / `.scene-label-key`, `.scene-svg` (+ `.node-core`, `.node-ring`, `.nodes text`, `.edges path`), `.scene-foot`, `@keyframes spin`, and the 900px hero media query.
- `app/page.tsx` — hero placeholder scaffold replaced with `<Hero />`; sections 02–08 remain placeholders.
- Phase 1 process-gate wiring + shared primitives (EPIC-01 §2, §3 item 0): `simple-git-hooks@^2.11.1` devDep wired via `prepare` script + `simple-git-hooks.pre-commit = pnpm process:check` map (ADR-0002). `components/sections/SectionHeader.tsx` — server component rendering the `.section-head` grid (num + title + rule + optional kicker) shared across all eight sections. `components/ui/ArrowLink.tsx` — server component wrapping `next/link` (or `<a>` for hash/external hrefs) and appending an `.arrow` span for reuse across case-link, more-link, CTA buttons. `lib/stats.ts` — typed loader over `public/data/stats.json` consumed server-side by `OpenSource` (Slice 1.7) and any future stat-surface.
- Phase 1 about slice (EPIC-01 §2 section 02, §3 item 3): `components/sections/About.tsx` — server component renders the `<SectionHeader num="02" title="About" />`, the striped-SVG portrait placeholder (reference `index.html:1197–1209`; a real `<Image src="/images/abhishek.webp" />` lands in Slice 2.2), the long-form bio (reference-verbatim copy matching `BIO_DRAFT.md §2` long variant — Abhishek's edited pass ships in Slice 2.2), and the four-row `about-meta` ledger (Now / Building / Co-founder / Writes). `app/globals.css` — ported `.about`, `.about-grid`, `.about-portrait`, `.about-kicker`, `.about-lede`, `.about-prose`, `.about-meta`, placeholder-portrait rules + `@media (max-width: 800px)` from `_reference/portfolio/styles.css:348–409`. `app/page.tsx` — `about` scaffold stub dropped; `<About />` takes its place while sections 03–08 remain scaffolds.
- Phase 1 services slice (EPIC-01 §2 section 05, §3 item 4): `components/sections/Services.tsx` — server component renders the `<SectionHeader num="05" title="What I can build for you" kicker={...} />` and three `<article class="service">` cards copied verbatim from the reference (S/01 Agent MVP 4–6 weeks, S/02 AI enablement for an MSME operation 8–12 weeks, S/03 Production-hardening a POC 3–6 weeks). Each card ships `service-num`, serif `<h3>`, mono `service-dur`, `service-lede`, and an In/Out/Fit `service-list`. `app/globals.css` — ported `.services`, `.service-grid` (3-column hairline-framed grid), `.service`, `.service header`, `.service-num` (note: distinct from `.section-num`, has `grid-column: 1 / -1` and different sizing), `.service h3`, `.service-dur`, `.service-lede`, `.service-list` rules + `@media (max-width: 860px)` from `_reference/portfolio/styles.css:576–648`. `app/page.tsx` — reorganised to interleave real components with `<Placeholder />` stubs in their reference positions so Services renders at slot 05 between scaffolded Writing and Process.
- Phase 1 process slice (EPIC-01 §2 section 06, §3 item 4): `components/sections/Process.tsx` — server component renders the inverted-ink feature-wall block (`.process-inner` on `var(--ink)` background in light mode, re-inverting to `var(--bg-2)` in dark mode). Left column: `06 · feature wall` eyebrow, Newsreader headline "Built in the open.", lede + italic sub. Right column: six-row `process-artifacts` list with `<code>` filenames in `--motion-node` green and right-aligned descriptions. Hover shifts row padding-left 4px → 14px (hairline indent slide). `app/globals.css` — ported `.process`, `.process-inner`, dark-mode overrides, `.process-title`, `.process-lede`, `.process-sub`, `.process-artifacts` list / anchor / code / span rules + `@media (max-width: 820px)` single-column collapse from `_reference/portfolio/styles.css:650–726`. Artifact hrefs currently `#` — Phase 4 rewires them to the real `/process`, `/llms.txt`, `/llms-full.txt`, `/sitemap.xml` routes. `app/page.tsx` — `process` scaffold dropped; `<Process />` renders at slot 06.
- Phase 1 contact slice (EPIC-01 §2 section 08, §3 item 8): `components/sections/Contact.tsx` — server component renders `08` eyebrow, Newsreader headline "Let's talk about your project.", the ~52ch lede, CTA row (primary `mailto:` button with arrow + ghost "Book a 20-minute call" which stays `href="#"` until Slice 5.3 wires Calendly), and a four-column `contact-links` DL (GitHub · LinkedIn · X · Résumé). External links open in a new tab with `rel="noopener noreferrer"`. `app/globals.css` — ported `.contact`, `.contact-inner`, `.contact-title`, `.contact-lede`, `.contact-cta`, `.contact-links` (+ 2×2 grid at ≤720px with adjusted borders) from `_reference/portfolio/styles.css:728–786`. Also dropped the now-unused `main > section[data-section-placeholder='true']` CSS rule since all eight sections now render real components. `app/page.tsx` — final scaffold dropped, `<Contact />` renders at slot 08; the local `<Placeholder />` helper is gone. All eight sections of the reference now render in the Next.js build.
- Phase 1 in-the-open slice (EPIC-01 §2 section 07, §3 item 6): `components/sections/OpenSource.tsx` — server component consuming `lib/stats.ts` at build time. Renders `<SectionHeader num="07" title="In the open" note={...} />` with the live-from-github pill replacing the default `.section-rule` (new `note` prop on `SectionHeader`). Then an `.os-inner` grid with: the big Newsreader contribution number in `--accent` (`stats.totalContributions.toLocaleString`), a 52-bar sparkline SVG generated server-side from `stats.weeks` (each bar width 7 / gap 3 / height scaled to `MAX_BAR_HEIGHT=90` / opacity ramped by `0.45 + 0.55 * (v/max)`), and a per-repo list with proportional commit-bar widths. A small `formatRelativeDays()` helper lives inside the component — no date library dependency. `app/globals.css` — ported `.opensource`, `.section-note` (+ `.section-note-link` hover), `.os-inner`, `.os-total`, `.os-label`, `.os-number`, `.os-sub`, `.os-spark`, `.os-spark-svg` (+ axis + rect fill), `.os-repos`, `.os-repo`, `.os-repo-name`, `.os-repo-bar`, `.os-repo-count`, `.os-repo-loading` rules + `@media (max-width: 820px)` from `_reference/portfolio/index.html:793–901` (inline stylesheet in the reference HTML, not styles.css). `components/sections/SectionHeader.tsx` — added optional `note?: ReactNode` prop that, when provided, renders a `.section-note` span in the third grid column instead of the default `.section-rule`. `app/page.tsx` — `open` scaffold dropped; `<OpenSource />` renders at slot 07. Live data still pending the `GH_STATS_TOKEN` GitHub secret; visual merge is independent of that.
- Phase 1 writing slice (EPIC-01 §2 section 04, §3 item 7): `components/sections/Writing.tsx` — server component with `<SectionHeader num="04" title="Writing" kicker="..." />`, three placeholder `<li class="post">` rows ported verbatim from `_reference/portfolio/index.html:1390–1408` (Apr 2026 Karpathy micrograd/makemore · Mar 2026 MSME notes · Feb 2026 TEI migration), and an "All writing →" `.more-link` footer. Phase-1 empty-state treatment — the placeholder posts are the empty state the reference itself ships; Phase-2 Slice 2.7 swaps this array for a server-side `getAllPosts('writing')` call reading real MDX from `content/writing/*.mdx`. `app/globals.css` — ported `.writing`, `.post-list`, `.post`, `.post-date`, `.post-title` (+ hover fill-underline trick via `background-image`), `.post-dek`, `.post-read`, `.more-link` + `@media (max-width: 720px)` from `_reference/portfolio/styles.css:512–573`. `app/page.tsx` — Writing scaffold dropped; `<Writing />` renders at slot 04 between `<Work />` and `<Services />`.
- Phase 1 work slice (EPIC-01 §2 section 03, §3 item 5): `components/sections/Work.tsx` — home-page server component renders `<SectionHeader num="03" title="Selected work" kicker={...} />` and four `<li class="case-item">` cards in canonical order: Neev (lead) → VeriCite → Bluehost · agents framework → curat.money. Each card carries case-meta (index · tag · year), case-body (title / italic dek / lede / spec DL / `<Link class="case-link">` arrow link into the detail page), and a per-case SVG `case-reel` placeholder ported verbatim from the reference (node-graph for Neev, retrieval-trace rectangles for VeriCite, latency-curve chart for Bluehost, compare-table rows for curat.money). Copy lifted verbatim from `_reference/portfolio/index.html:1241–1377`. `components/sections/CaseStudyStub.tsx` — shared stub rendered by all four `/work/[slug]` pages: meta row + title + dek + lede + spec DL + "in preparation" notice with a `mailto:` escape. `app/work/neev/page.tsx`, `app/work/vericite/page.tsx`, `app/work/bluehost-agents/page.tsx`, `app/work/curat-money/page.tsx` — four route stubs each exporting Metadata and rendering `<CaseStudyStub slug={...} />`. Exists so the Work card "Read the case study →" links never 404 during the Phase 2 content gap; Phase 2 replaces these four static pages with `app/work/[slug]/page.tsx` reading from `content/case-studies/*.mdx`. `app/globals.css` — ported `.work`, `.case-list`, `.case-item`, `.case-item--lead`, `.case-meta`, `.case-index`, `.case-tag`, `.case-year`, `.case-title`, `.case-dek`, `.case-lede` (+ inline `code` styling), `.case-spec` dt/dd grid, `.case-link` (+ `.arrow` hover translate), `.case-reel` (sticky at ≥960px), generic `.placeholder-label`/`.placeholder-sublabel`, `.placeholder-reel` rules, and a new `.work-stub*` block for the detail-page layout, from `_reference/portfolio/styles.css:411–510, 816–830`. Also cleaned up the portrait-scoped `.placeholder-portrait .placeholder-label/.placeholder-sublabel` overrides from Slice 1.2 in favour of the reference's generic rules (portrait still uses the same class names; visual result matches). `app/page.tsx` — Work scaffold dropped; `<Work />` renders at slot 03.

### Deferred
- Three.js agent-graph canvas (the ~250-line IIFE from the reference) — ships in a dedicated slice. Hero currently renders the static SVG graph verbatim from the reference fallback markup, which is what `prefers-reduced-motion: reduce` / `[data-motion="off"]` users would see anyway.
- Tagline A/B/C live swap — needs `TweakBridge` (dedicated slice per HANDOFF §6). Spans are authored with the reference's valueless `data-tagline-*` markers + `hidden` on B/C so TweakBridge only has to toggle `hidden`.
- Next.js 16.2 scaffold: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `middleware.ts`, `eslint.config.js`.
- Design token foundation in `app/globals.css` mirroring the Claude Design reference (parchment, ink, forest accent, Newsreader + Plus Jakarta Sans + JetBrains Mono).
- Agent-readiness scaffold: `public/robots.txt` with Content Signals, `public/llms.txt`, `public/.well-known/agent-skills/index.json`, `public/.well-known/mcp.json`, Link-header middleware.
- `scripts/fetch-github-stats.mjs` + `.github/workflows/stats.yml` (weekly refresh).
- `scripts/process-gate.mjs` real policy (R1 code→CHANGELOG, R2 structural→ADR/CHANGELOG, R3 EPM→ROADMAP). Bypass via `SKIP_PROCESS_GATE=1`. See ADR-0002.
- ADR-0002 — process-gate policy + git-hook tool (`simple-git-hooks`, wired in Phase 1).
- EPIC-01 — pixel-parity plan for Phase 1 (`docs/epm/EPIC-01-pixel-parity.md`).
- `turbopack.root = process.cwd()` in `next.config.ts` to silence the multi-lockfile warning.
- `app/layout.tsx`: swapped Google Fonts `<link>` for `next/font/google` (Newsreader + Plus Jakarta Sans + JetBrains Mono). Self-hosted, eliminates external handshake, clears the `@next/next/no-page-custom-font` warning. `app/globals.css` resolves `--serif/--sans/--mono` through the next/font CSS variables.
- `vercel.json` — pins `framework: "nextjs"`, `buildCommand: "pnpm run build"`, `installCommand: "pnpm install --frozen-lockfile"`, `outputDirectory: ".next"`. Fixes the SvelteKit-era dashboard setting (`outputDirectory: "build"`) that was still live on the Vercel project and made every PR preview fail with `No Output Directory named "build" found after the Build completed`. In-repo config overrides dashboard drift and survives any future framework preset changes.
- `next-mdx-remote` bumped `^5.0.0 → ^6.0.0`. Vercel's deploy pipeline blocks 5.x as vulnerable (`Vulnerable version of next-mdx-remote detected (5.0.0). Please update to version 6.0.0 or later.`). 6.0.0 is API-compatible for our usage (we don't call it yet — MDX pipeline lands in Phase 2).

### Changed
- `docs/epm/EPIC-01-pixel-parity.md` + `docs/ROADMAP.md`: Phase-1 build order reseq'd — Hero Three.js canvas (agent-graph live scene) and the Wanderer r3f full port both move to Phase 5 (Slices 5.1b / 5.1c, alongside the R3F 9 + drei 10 peer-dep bump). Phase 1 ships the static SVG agent-graph (already live) and, via Slice 1.9, the SVG-only Wanderer + `#companion` mount wiring. Rationale: decouple pixel parity from the peer-dep upgrade so a regression in one doesn't block the other; keep the Phase 1 bundle under budget until content polish settles.
- Repository migrated from SvelteKit to Next.js 16.2. Prior state preserved at tag `legacy-v1-final`.
- Renamed `docs/prd.md` → `docs/PRD.md` to match the PascalCase convention used everywhere else in `docs/`.
- `next.config.ts`: moved `typedRoutes` out of `experimental` (stable in Next 16.2).
- `package.json`: `lint` script switched from `next lint` (removed in Next 16) to `eslint .`.
- `package.json`: TypeScript pin corrected `6.0.0 → 6.0.3` — `6.0.0` was never published; `6.0.x` patch drift is the intended pin.
- `eslint.config.js`: named the default export to satisfy `import/no-anonymous-default-export`.
- `tsconfig.json`: Next 16.2 auto-applied `jsx: react-jsx` and added `.next/dev/types/**/*.ts` to `include` on first build; committed as-is.

### Removed
- SvelteKit source: `src/**`, `static/**`, `svelte.config.js`, `vite.config.ts`, the prior `pnpm-lock.yaml`. Reachable at the `legacy-v1-final` tag.
