# Agent readiness snapshot — 2026-05-19

**Target:** `https://akaushik.org/`
**Method:** `curl -sIL` for headers + manual verification of each well-known surface.
**Baseline:** first dated snapshot since the post-launch site shipped.

## Headers on the home page

```
HTTP/2 200
content-type: text/html; charset=utf-8
link: </llms.txt>; rel="describedby"; type="text/markdown",
      </llms-full.txt>; rel="describedby"; type="text/markdown",
      </sitemap.xml>; rel="sitemap"; type="application/xml",
      </.well-known/agent-skills/index.json>; rel="describedby"; type="application/json",
      </.well-known/mcp.json>; rel="describedby"; type="application/json",
      </.well-known/api-catalog>; rel="api-catalog"; type="application/linkset+json"
referrer-policy: strict-origin-when-cross-origin
x-content-type-options: nosniff
x-robots-tag: index, follow
strict-transport-security: max-age=63072000
```

All six `Link` rels present in one header. Matches `middleware.ts` shape. ✅

## Discoverability

| Surface | Status | Notes |
|---|---|---|
| `/robots.txt` | live | Allow + sitemap + Content-Signal directives served from `app/robots.txt/route.ts` |
| `/sitemap.xml` | live | Dynamic build from `app/sitemap.ts`; includes home, /work, /writing, every case-study slug, every writing slug |
| `Link` headers | live | All six rels emitted from `middleware.ts` (verified above) |
| `/.well-known/api-catalog` | live | `app/.well-known/api-catalog/route.ts` |
| `/.well-known/agent-skills/index.json` | live | sha256 digest computed by `scripts/build-agent-skills-index.ts` prebuild hook |
| `/.well-known/agent-skills/hire-me/SKILL.md` | live | Hire-me Agent Skill content |
| `/.well-known/mcp.json` | live (planned status) | `endpoint` removed in PR-1 of gap-analysis plan; `status: planned` signals deferred to v1.1 |

## Content negotiation

| Surface | Status | Notes |
|---|---|---|
| `/llms.txt` | live | `app/llms.txt/route.ts`, force-static, 1h cache |
| `/llms-full.txt` | live | `app/llms-full.txt/route.ts`, force-static, 5min cache; assembles About + Services + all case studies + all writing |
| Pattern A (`Accept: text/markdown`) | live | Middleware rewrites `/work/<slug>` and `/writing/<slug>` to `…/md` |
| Pattern B (`.md` suffix) | live | `app/work/[slug]/md/route.ts` + `app/writing/[slug]/md/route.ts` |

## Bot access control

| Surface | Status | Notes |
|---|---|---|
| `robots.txt` Content-Signal | live | `search=yes, ai-input=yes, ai-train=yes` per ADR + portfolio sales-asset intent |
| Named-AI-bot blocks | not applied | `User-agent: *` only — intentional permissive default |

## Capabilities

| Surface | Status | Notes |
|---|---|---|
| `/api/openapi.json` | live | OpenAPI 3.1 spec describing every public route handler |
| `/api/case-studies` | live | JSON listing |
| `/api/writing` | live | JSON listing |
| `/api/docs` | pending | Tracked by PR-5 of the gap-analysis plan |
| `/api/mcp` | deferred to v1.1 | `mcp.json` now correctly advertises this as planned |

## Legacy host verification

| Host | Method | Result |
|---|---|---|
| `developerabhishek.live` | `curl -sIL https://developerabhishek.live/` | No HTTP response received (connection completed silently, likely DNS or origin not configured). **Action:** owner to verify whether the registration is still active and whether the `vercel.json` 308 redirect described in ADR-0003 §15.D is deployed. |
| `akaushik.dev` | `curl -sI https://akaushik.dev/` | 200 OK; serves the same content as `akaushik.org` (matching `etag: a58ae34222cb101720cc0135402c5b81`). This is **co-serving**, not a 301 redirect. Per ADR-0003 the intent is a 301 to canonical; the Vercel project currently has both domains attached to the same deployment. **Action:** owner to configure `akaushik.dev` as a permanent 308 redirect in the Vercel project domain settings. |
| `akaushik.org` | `curl -sI https://akaushik.org/` | 200 OK with full agent-readiness Link header. Canonical. ✅ |

## Pending owner actions (do not block this snapshot)

- Verify `developerabhishek.live` registration + redirect status.
- Configure `akaushik.dev` as 308 redirect rather than co-serve.
- Run `isitagentready.com` against `https://akaushik.org/` in a browser and save the dimension-score screenshot alongside this file.
- Submit GSC Change of Address (`developerabhishek.live` → `akaushik.org`) — separate handoff item.

## Result

All required agent-readiness surfaces respond as specified. The deferred items (`/api/docs`, `/api/mcp`) are correctly signalled — the docs page is in-flight; the MCP endpoint is honestly marked planned. Next snapshot is opportunistic — the scheduled `seo-monthly-health` task will refresh this file on its first run; PRs that touch `middleware.ts`, `app/.well-known/`, or `lib/structured-data.ts` should also append a fresh snapshot at the next session.
