# dependencies
node_modules
.pnpm-store

# next.js
.next/
out/
build/

# vercel
.vercel

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*.local
.env
local.env
*.env

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# local agent/tooling config — .claude/* is blanket-ignored; the allowlist
# below tracks the bits that ARE project state and should travel with the repo.
.claude/*
!.claude/rules/
!.claude/skills/
!.claude/hooks/
!.claude/hooks/*
!.claude/settings.json
!.claude/primers/
!.claude/primers/*
!.claude/commands/
.claude/settings.local.json
context-log.md

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Lighthouse CI local artefacts (lhci writes here; CI uploads via the action)
.lighthouseci/

# Codex parity — .agents and .codex are NOT under the .claude/* blanket;
# explicit allowlist for clarity.
!.agents/
!.codex/

# --- Trellis inheritance symlinks (7-skill set + presets + primer commands) — per-machine; regenerated by onboard-project.sh ---
# Targets are absolute paths under $TRELLIS_ROOT, which differs on every developer's
# machine. Tracking these in git produces cross-machine merge conflicts. Each developer
# recreates them post-clone via:
#   ~/projects/trellis-instance/scripts/onboard-project.sh <project-path>
# See engineering-process.md (in the Trellis canonical repo) §4.2 + §10.5 for the full tracking policy.
#
# Note: .claude/primers/INDEX.md is NOT ignored — it is project-state (copied, not
# symlinked) and should be tracked. Individual primer files (.claude/primers/<slug>.md)
# are also project-state and tracked. Only the symlinked commands below are ignored.
.claude/rules/trellis.md
.claude/rules/preset-*.md
.claude/skills/process-gate
.claude/skills/security-gate
.claude/skills/clarify
.claude/skills/spec
.claude/skills/plan
.claude/skills/tasks
.claude/skills/analyze
.claude/commands/primer.md
.claude/commands/primer-refresh.md
.claude/commands/primer-check.md
.agents/rules/trellis.md
.agents/rules/preset-*.md
.agents/skills/process-gate
.agents/skills/security-gate
.agents/skills/clarify
.agents/skills/spec
.agents/skills/plan
.agents/skills/tasks
.agents/skills/analyze
.agents/commands/primer.md
.agents/commands/primer-refresh.md
.agents/commands/primer-check.md
# --- end Trellis fragment ---
